version: '3.8'

services:
  app:
    image: teachertribe-elearn
    container_name: teachertribe-elearn
    build:
      context: .
      dockerfile: ./Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules

      